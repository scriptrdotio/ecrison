const appIoTPrefix = "https://eappiot-api.sensbysigma.com";
const clientUrlPrefix = appIoTPrefix + "/clientapi/v1"; // Client API URL prefix

const token = "";// Default API key

const errorCodeList = {
    "missingParameter" : "MISSING_PARAMETER",
    "invalidAction": "INVALID_ACTION",
    "notFound": "ENTITY_NOT_FOUND"
};
